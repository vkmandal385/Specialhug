<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Happy Hug Day 💖</title>
<link href="https://fonts.googleapis.com/css2?family=Pacifico&family=Poppins:wght@400;600&display=swap" rel="stylesheet">
<style>
  body {
    margin: 0;
    padding: 0;
    font-family: 'Poppins', sans-serif;
    overflow: hidden;
    background: linear-gradient(135deg, #ffdde1, #ee9ca7);
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    color: #fff;
    text-align: center;
    position: relative;
  }

  /* Hearts */
  .heart {
    position: absolute;
    width: 20px;
    height: 20px;
    background: red;
    transform: rotate(45deg);
    animation: float 6s infinite;
  }
  .heart::before,
  .heart::after {
    content: '';
    position: absolute;
    width: 20px;
    height: 20px;
    background: red;
    border-radius: 50%;
  }
  .heart::before { top: -10px; left: 0; }
  .heart::after { left: 10px; top: 0; }

  @keyframes float {
    0% { transform: translateY(0) rotate(45deg); opacity: 1; }
    100% { transform: translateY(-600px) rotate(45deg); opacity: 0; }
  }

  .card {
    background: rgba(255, 255, 255, 0.1);
    padding: 40px 30px;
    border-radius: 25px;
    box-shadow: 0 8px 20px rgba(0,0,0,0.2);
    backdrop-filter: blur(10px);
    max-width: 400px;
    position: absolute;
    opacity: 0;
    transition: all 0.5s ease;
  }

  .card.active {
    opacity: 1;
    transform: translateX(0);
    z-index: 10;
  }

  .card h1 {
    font-family: 'Pacifico', cursive;
    font-size: 2rem;
    margin-bottom: 15px;
    color: #fff;
  }

  .card p {
    font-size: 1.1rem;
    margin-bottom: 15px;
    color: #fff;
  }

  .card img {
    width: 180px;
    border-radius: 50%;
    margin-bottom: 15px;
    border: 3px solid #fff;
  }

  .card button {
    background: #ff6f91;
    border: none;
    padding: 12px 20px;
    border-radius: 50px;
    color: #fff;
    font-size: 1rem;
    cursor: pointer;
    transition: 0.3s;
    font-weight: 600;
    margin: 5px;
  }

  .card button:hover {
    background: #ff477e;
    transform: scale(1.1);
  }
</style>
</head>
<body>

<!-- Page 1 -->
<div class="card active" id="page1">
  <h1>Happy Hug Day Sweetheart 😘</h1>
  <img src="cute-hug.png" alt="Cute Hug">
  <p> Mai aapko virtual hug bej raha hu aap isse v feel kar lena! 🤗💖</p>
  <button onclick="nextPage(2)">Next ➡️</button>
</div>

<!-- Page 2 -->
<div class="card" id="page2">
  <h1>Special Hug 🤍</h1>
  <img src="couple-hug.png" alt="Couple Hug">
  <p>"Aapki hug meri favourite therapy hai 😍"</p>
  <button onclick="nextPage(3)">Next ➡️</button>
</div>

<!-- Page 3 -->
<div class="card" id="page3">
  <h1>Forever Yours 💕</h1>
  <img src="love-hug.png" alt="Love Hug">
  <p>"Aapka hug toh bin Mosaam ki barsat la sakti hai 🌧️ ☀️"</p>
  <button onclick="nextPage(4)">Next ➡️</button>
</div>

<!-- Page 4: Hindi Shayari Book -->
<div class="card" id="page4">
  <h1>For you my love 📖</h1>
  <p id="shayariText">"Aapki muskaan meri khushi hai 💖"</p>
  <div>
    <button onclick="prevShayari()">⬅️ Previous</button>
    <button onclick="nextShayari()">Next ➡️</button>
  </div>
</div>

<script>
  // Heart animation
  const colors = ['#ff6f91','#ffb347','#ffcc70','#ff6f91'];
  function createHeart(){
    const heart = document.createElement('div');
    heart.classList.add('heart');
    heart.style.left = Math.random()*window.innerWidth+'px';
    heart.style.backgroundColor = colors[Math.floor(Math.random()*colors.length)];
    heart.style.width = heart.style.height = (10+Math.random()*20)+'px';
    document.body.appendChild(heart);
    setTimeout(()=>{ heart.remove() },6000);
  }
  setInterval(createHeart,300);

  // Page switching
  function nextPage(num){
    const pages=['page1','page2','page3','page4'];
    pages.forEach(id=>document.getElementById(id).classList.remove('active'));
    document.getElementById('page'+num).classList.add('active');
  }

  // Shayari book
  const shayariList = [
    "Aapki muskaan meri khushi hai 💖",
    "Har hug aapse meri duniya complete karta hai 🌸",
    "Aapke bina toh mera dil adhoora sa lagta hai 🤗",
    "Mere liye aapka hug sabse bada gift hai 🎁",
    "Aapki baahon mein sab kuch perfect lagta hai 💕",
    "I love you meri jaaan❤️",
    "I love you itna jitna aap soch v nhi Sakti😘❤️"
  ];
  let currentShayari = 0;
  const shayariText = document.getElementById('shayariText');

  function nextShayari(){
    if(currentShayari < shayariList.length -1){
      currentShayari++;
      shayariText.textContent = shayariList[currentShayari];
    }
  }

  function prevShayari(){
    if(currentShayari > 0){
      currentShayari--;
      shayariText.textContent = shayariList[currentShayari];
    }
  }
</script>

</body>
</html>
